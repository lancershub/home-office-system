<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yourcompany.reception.mapper.DepartmentMapper">

    <select id="selectAll" resultType="com.yourcompany.reception.entity.Department">
        SELECT d.*, i.inst_name
        FROM department_info d
        LEFT JOIN institution_info i ON d.inst_id = i.id
        ORDER BY d.create_time DESC
    </select>

    <insert id="insert" parameterType="com.yourcompany.reception.entity.Department">
        INSERT INTO department_info (dept_name, dept_desc, manager_id, inst_id, create_time)
        VALUES (#{dept_name}, #{dept_desc}, #{manager_id}, #{inst_id}, NOW())
    </insert>

    <update id="update" parameterType="com.yourcompany.reception.entity.Department">
        UPDATE department_info
        SET dept_name = #{dept_name},
        dept_desc = #{dept_desc},
        manager_id = #{manager_id},
        inst_id = #{inst_id}
        WHERE id = #{id}
    </update>

    <delete id="delete" parameterType="java.lang.Integer">
        DELETE FROM department_info WHERE id = #{id}
    </delete>

</mapper>